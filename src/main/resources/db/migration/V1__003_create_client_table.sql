CREATE TABLE `Client` (
                            `Client_id` smallint,
                            `Name` varchar(255),
                            `Address` varchar(200),
                            `Phone_Num` varchar(64),
                            `Proj_id` smallint

);
